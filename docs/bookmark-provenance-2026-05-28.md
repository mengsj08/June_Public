# Bookmark Provenance — 2026-05-28

This public source atlas was derived from a local bookmark-intake review and
then manually filtered into public, reusable information sources.

The raw browser bookmark export is intentionally not committed to this public
repository because it may contain private, login-state, paid-service, personal,
or local-network links.

Public derived assets:

- `sources.json`: structured source data used by the landing page.
- `index.html`: GitHub Pages landing page.

Private local backup:

- `docs/private/bookmark_exports/bookmarks_5_28_26.html`

The `docs/private/` directory is gitignored.
